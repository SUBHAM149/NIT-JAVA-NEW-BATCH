# 🚀 Create Web Project → Convert to Maven Project (Step-by-Step)   [SOME IMPORTANT THING IN DOWN PLZ SCROLL TO DOWN...!!!]

## 🔹 PART 1: Create Dynamic Web Project
### Step 1: Open Eclipse
* Open Eclipse IDE
* Go to **File → New → Dynamic Web Project**

### Step 2: Configure Project

* Project Name: `MyWebApp`
* Target Runtime: Select Apache Tomcat
* Dynamic Web Module Version: 4.0 (or default)

👉 Click **Next → Next**

### Step 3: Web Module Settings

* Context Root: `MyWebApp`
* Content Directory: `WebContent`

👉 Click **Finish**

---

## 🔹 PART 2: Project Structure (Initial)

```
MyWebApp/
 ├── Java Resources/
 ├── WebContent/
 │    ├── WEB-INF/
 │    │     └── web.xml
 │    ├── index.jsp
```

---

## 🔹 PART 3: Convert to Maven Project

### Step 1: Right Click Project

* Right click on `MyWebApp`
* Click: **Configure → Convert to Maven Project**

---

### Step 2: Enter Maven Details

Fill:

* Group Id: `com.sub`
* Artifact Id: `MyWebApp`
* Version: `1.0`
* Packaging: `war`

👉 Click **Finish**

---

## 🔹 PART 4: Maven Project Structure (After Conversion)

```
MyWebApp/
 ├── src/
 │    ├── main/
 │    │    ├── java/
 │    │    ├── webapp/
 │    │         ├── WEB-INF/
 │    │         │    └── web.xml
 │    │         └── index.jsp
 ├── pom.xml
```

---

## 🔹 PART 5: Move Files Properly

Move files from old structure:

* Move `WebContent/*` → `src/main/webapp/`

---

## 🔹 PART 6: Add Dependencies in pom.xml

Example:

```xml
<dependencies>

    <!-- Servlet API -->
    <dependency>
        <groupId>jakarta.servlet</groupId>
        <artifactId>jakarta.servlet-api</artifactId>
        <version>6.0.0</version>
        <scope>provided</scope>
    </dependency>

</dependencies>
```

---

## 🔹 PART 7: Add Tomcat Plugin (Optional)

```xml
<build>
    <plugins>
        <plugin>
            <groupId>org.apache.tomcat.maven</groupId>
            <artifactId>tomcat7-maven-plugin</artifactId>
            <version>2.2</version>
        </plugin>
    </plugins>
</build>
```

---

## 🔹 PART 8: Update Project

* Right click project
* Click **Maven → Update Project (Alt + F5)**

---

## 🔹 FINAL RESULT ✅

✔ Web project created
✔ Converted to Maven
✔ Proper folder structure
✔ Ready to run on Tomcat

---

## 🔥 BONUS TIP

If servlet not working:

* Add annotation: `@WebServlet("/home")`
* Or configure in `web.xml`

---

## 🎯 SUMMARY

1. Create Dynamic Web Project
2. Right Click → Convert to Maven
3. Move files to `src/main/webapp`
4. Add dependencies
5. Update project

---
💡 Now your project is **industry-standard Maven-based Web App**


The Mazor Things Of Servlet Project
-----------------------------------
*In The Servlet Projetc If You See The Program Structure png not web.xml file and pom.xml file OR IF You see Here In the file i added you Create Initialy And Past IT.



